import { describe, expect, it } from 'vitest'
import type { ReportDocument } from '../src/shared/contracts'
import { escapeHtml, renderReportHtml } from '../src/main/report-renderer'
const document:ReportDocument={schemaVersion:1,id:'11111111-1111-4111-8111-111111111111',title:'<script>alert(1)</script>',subtitle:'Evidence',template:'research-light',revision:1,createdAt:'2026-07-22T00:00:00.000Z',updatedAt:'2026-07-22T00:00:00.000Z',blocks:[{id:'22222222-2222-4222-8222-222222222222',type:'rich-text',layout:{mode:'structured',span:2,page:1,x:0,y:0,width:100,height:10},style:{fontFamily:'sans',fontSize:18,fontWeight:400,italic:false,underline:false,alignment:'left',color:'#172033',lineHeight:1.5},text:'<img src=x onerror=alert(1)>\nSafe line'},{id:'33333333-3333-4333-8333-333333333333',type:'image',layout:{mode:'structured',span:2,page:1,x:0,y:0,width:100,height:10},style:{fontFamily:'sans',fontSize:12,fontWeight:400,italic:false,underline:false,alignment:'left',color:'#172033',lineHeight:1.4},imagePath:'/definitely/missing/image.png',caption:'Missing'}]}
describe('report renderer',()=>{it('escapes user-authored markup and uses a restrictive CSP',()=>{const rendered=renderReportHtml(document);expect(rendered.html).toContain("default-src 'none'");expect(rendered.html).toContain('&lt;script&gt;alert(1)&lt;/script&gt;');expect(rendered.html).not.toContain('<img src=x onerror=alert(1)>');expect(rendered.html).toContain('&lt;img src=x onerror=alert(1)&gt;')});it('produces visible preflight warnings for missing local media',()=>{const rendered=renderReportHtml(document);expect(rendered.warnings).toEqual([expect.stringContaining('Missing image')]);expect(rendered.html).toContain('Image unavailable')});it('escapes all HTML-significant characters',()=>{expect(escapeHtml(`<&>"'`)).toBe('&lt;&amp;&gt;&quot;&#39;')})})
