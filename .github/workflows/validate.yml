name: Validate ledger tooling

on:
  pull_request:
    paths:
      - "tools/**"
      - "tests/**"
      - "web/**"
      - "object-detection/**"
      - "README.md"
      - "README.en.md"
      - "AGENTS.md"
      - "release_policy.py"
      - "project-contract.json"
      - "config/**"
      - "docs/**"
      - "requirements-analytics.txt"
      - "requirements-forecast.txt"
      - "requirements-visual-analysis.txt"
      - "requirements-yolo.txt"
      - "requirements-nanodet.txt"
      - ".github/workflows/**"
  push:
    branches: [main]
    paths:
      - "tools/**"
      - "tests/**"
      - "web/**"
      - "object-detection/**"
      - "README.md"
      - "README.en.md"
      - "AGENTS.md"
      - "release_policy.py"
      - "project-contract.json"
      - "config/**"
      - "docs/**"
      - "requirements-analytics.txt"
      - "requirements-forecast.txt"
      - "requirements-visual-analysis.txt"
      - "requirements-yolo.txt"
      - "requirements-nanodet.txt"
      - ".github/workflows/**"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements-analytics.txt
            requirements-forecast.txt
            requirements-visual-analysis.txt
            requirements-yolo.txt
            requirements-nanodet.txt
      - name: Install Python, detector, and FFmpeg test dependencies
        run: |
          set -euo pipefail
          python -m pip install \
            -r requirements-analytics.txt \
            -r requirements-forecast.txt \
            -r requirements-visual-analysis.txt \
            -r requirements-yolo.txt \
            -r requirements-nanodet.txt
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends ffmpeg
          ffmpeg -version | head -n 1
          ffprobe -version | head -n 1
      - name: Validate synchronized project contract
        shell: bash
        run: |
          set -o pipefail
          python tools/validate_project_contract.py 2>&1 | tee contract-validation.log
      - run: python -m compileall tools tests
      - name: Run complete unittest suite
        shell: bash
        run: |
          set -o pipefail
          python -m unittest discover -s tests -v 2>&1 | tee unittest.log
      - name: Verify pinned YOLOX-Tiny ONNX execution
        run: python tools/yolo_model_smoke.py
      - name: Verify pinned NanoDet-Plus ONNX execution
        run: python tools/nanodet_model_smoke.py
      - name: Upload validation diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: validation-logs-${{ github.run_id }}
          path: |
            contract-validation.log
            unittest.log
          if-no-files-found: warn
          retention-days: 7
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Install web dependencies
        run: npm install --prefix web --package-lock=false --no-audit --no-fund
      - name: Build production Pages site
        run: npm run build --prefix web
      - name: Validate Pages routes and artifact URLs
        run: python tools/validate_site_build.py
